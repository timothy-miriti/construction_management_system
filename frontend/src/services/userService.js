// Note: no GET /users endpoint exists on the backend yet.
// Manager/worker/uploader fields currently take a raw numeric user ID.
// A future improvement: add GET /api/users (admin-only) and swap these
// raw ID inputs for real dropdowns.
export const userService = {};